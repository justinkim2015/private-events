<nav class="navbar is-light">
  <div class="navbar-start">
    <h1 class="navbar-item is-size-1 ml-3">Events!</h1>
  </div>
  <div class="navbar-end mr-3">
    <div class="navbar-item"><%= link_to "Home", root_path %></div>
    <div class="navbar-item"><%= link_to "New Event", new_event_path %></div>
    <% if user_signed_in? %>
      <div class="navbar-item "><%= link_to "Sign out", destroy_user_session_path, data: { turbo_method: :delete } %></div>
      <div class="navbar-item"><%= link_to "Profile", user_path(current_user.id) %></div>
    <% else %>
      <div class="navbar-item"><%= link_to "Sign in", new_user_session_path %></div>
      <div class="navbar-item"><%= link_to "Profile", new_user_session_path %></div>
    <% end %>
  </div>
</nav>
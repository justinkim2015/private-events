
<ul>
  <li><%= p @event.place %></li>
  <li><%= p @event.date %></li>
  <li><%= p @event.creator.email %></li>
</ul>

<%= button_to "Attend", event_attendees_path(event_attendee: {attended_event_id: @event.id, attendee_id: current_user.id}) %>

<%# You can create a button in your events.show view that can submit to 
the create route for the through table controller. And yeah, I'd advise 
doing it that way over having the user click twice to attend  %>

<%# name = "event_attendee"  %>
